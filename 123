local Fluent = loadstring(game:HttpGet("https://github.com/dawid-scripts/Fluent/releases/latest/download/main.lua"))()
local Vim = game:GetService("VirtualInputManager")
local Lighting = game:GetService("Lighting")
local Terrain = workspace:FindFirstChildOfClass("Terrain")

local Window = Fluent:CreateWindow({
    Title = "EchOpp_",
    SubTitle = "Auto Skill & Misc",
    TabWidth = 160,
    Size = UDim2.fromOffset(450, 350),
    Acrylic = true,
    Theme = "Dark"
})

local Tabs = {
    Main = Window:AddTab({ Title = "Auto Key", Icon = "keyboard" }),
    Misc = Window:AddTab({ Title = "Misc", Icon = "settings" })
}

local Options = {}

# AUTO SKILL
local Keys = {"Z", "X", "C", "V", "F"}

local function CreateAutoKey(key)
    task.spawn(function()
        while true do
            if Options["Auto" .. key] and Options["Auto" .. key].Value then
                Vim:SendKeyEvent(true, Enum.KeyCode[key], false, game)
                task.wait(0.05)
                Vim:SendKeyEvent(false, Enum.KeyCode[key], false, game)
            end
            task.wait(0.1)
        end
    end)
end

for _, key in ipairs(Keys) do
    Options["Auto" .. key] = Tabs.Main:AddToggle("Auto" .. key, {Title = "Tự động bấm phím " .. key, Default = false })
    CreateAutoKey(key)
end

# MISC

Tabs.Misc:AddParagraph({
    Title = "idk",
    Content = "bla bla"
})

Tabs.Misc:AddButton({
    Title = "Boost FPS (Xóa Texture/Effect)",
    Description = "Xóa các Texture, Effect của Game (Không thể hoàn tác trừ khi reset game)",
    Callback = function()
        Window:Dialog({
            Title = "Xác nhận",
            Content = "Bạn có chắc chắn muốn xóa toàn bộ Texture và Effect không? ",
            Buttons = {
                {
                    Title = "Đồng ý",
                    Callback = function()
                        -- Tắt
